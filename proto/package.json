{
  "scripts": {
    "generate": "rm -rf ../packages/proto/src/gen ../CheckMate/proto && mkdir -p ../packages/proto/src/gen ../CheckMate/proto && buf generate",
    "format": "clang-format --style=google -i src/*.proto"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.34.0"
  }
}
