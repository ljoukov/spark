{
  "name": "spark-proto-defs",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "generate": "rm -rf ../packages/proto/src/gen ../CheckMate/proto && mkdir -p ../packages/proto/src/gen ../CheckMate/proto && buf generate && rm -rf ../CheckMate/proto/google",
    "format": "clang-format --style=google -i src/*.proto"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.34.0"
  }
}
