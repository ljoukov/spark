export const SPARK_GRADER_SUMMARY_PATH =
  "grader/output/run-summary.json" as const;
export const SPARK_GRADER_SHEET_PATH = "grader/output/sheet.json" as const;
export const SPARK_GRADER_UPLOADS_MANIFEST_PATH =
  "grader/uploads/index.json" as const;

export function buildSparkGraderAgentPrompt(options?: {
  summaryPath?: string;
  sheetPath?: string;
}): string {
  const summaryPath = options?.summaryPath ?? SPARK_GRADER_SUMMARY_PATH;
  const sheetPath = options?.sheetPath ?? SPARK_GRADER_SHEET_PATH;
  return [
    "Grade and process student uploaded work and related documents such as problem statements and official solutions.",
    "",
    "Read and follow these files first:",
    "- brief.md",
    "- request.json",
    "- grader/task.md",
    `- ${SPARK_GRADER_UPLOADS_MANIFEST_PATH}`,
    "- Respect request.json input.referenceSourcePolicy for online-search permissions.",
    "- Use `list_workspace_dir`, `read_workspace_file`, `grep_workspace_files`, and `write_workspace_file` for workspace text files and grader JSON artifacts. Do not use `extract_text` to read JSON/Markdown workspace files.",
    "- Keep answer capture separate from solving: first determine what the student visibly selected/wrote, then establish the correct-answer baseline, then mark.",
    "- For objective or multiple-choice work, record a selected option only when there is an explicit circled, ticked, shaded, boxed, or otherwise clearly selected answer mark. Ignore unrelated scribbles, crossings-out, workings, and marks on diagrams. If no selected option is visible, record the answer as blank/no-answer; never infer it from solving the question.",
    "- For identified public papers, use official answer keys, mark schemes, or official solutions when referenceSourcePolicy permits official reference lookup. If lookup is disallowed or no official source is available, self-solve carefully at the student's level.",
    "- Keep official/reference problem statements verbatim where possible; do not rewrite them into cleaned canonical wording.",
    "- Preserve original worksheet structure when the uploads are already a printed paper: keep shared stems via `group`, keep source numbering in `displayNumber`, keep MCQs/tables structured, and keep source sections non-redundant rather than inventing synthetic wrappers like `S1`.",
    "- Represent every assessed question and subquestion from the source paper in source order, including blank or unanswered items. Do not decide to include only answered items, only confident grades, or only questions with visible student writing.",
    '- If the run is source-paper-only and no student answers/submission are present, render the source as an unanswered worksheet rather than solving it: skip solution-baseline derivation unless the user explicitly asked for answers, leave answers blank (use `""` for unanswered MCQ and answer-bank selections), set `review.mode` to `"awaiting_answers"`, use review status labels such as `Awaiting answer`, omit per-question `review.questions[questionId].score`, keep per-question notes empty, do not mark it as a completed zero-score attempt, and do not expose an answer key in visible prompts, review notes, or reference markdown unless the user explicitly asked for answers.',
    "- In a source-paper-only/no-student run, do not output derived answers, likely answers, correct MCQ options, worked solutions, or answer-key notes anywhere. The task is source transcription and worksheet rendering, not solving.",
    "- For source-paper-only/no-student runs, omit `officialSolutionMarkdown` and grading-rationale reference fields instead of writing placeholder text such as `No official solution was included`; keep only source/problem/provenance references needed to audit the transcription.",
    "- `grader/output/transcription.md` must include both student-answer transcription and source problem-statement transcription when a printed/source paper is uploaded. Put the source transcript under the exact heading `## Source problem-statement transcription`. The source transcription must record printed root stems, interstitial context, subquestion prompts, table labels, figure labels, and MCQ option labels/text in source order; an audit note alone is not enough.",
    '- When a paper uses root/subpart numbering such as `01`, `01.1`, `01.2`, represent the root as a `group` entry (`type: "group", displayNumber: "01", prompt: "root stem...", questions: [...]`) and place the subparts inside `questions[]` with `displayNumber: "01.1"`, `displayNumber: "01.2"`, etc.; do not list those subparts flat in a section.',
    "- Put only the source text that appears after the root number and before the first subpart in the root `group.prompt`. Put interstitial text that appears between later subparts into the following child prompt, and do not duplicate the root stem inside the first child prompt.",
    "- Make worksheet sections useful as collapsible navigation: use real source sections when present; otherwise use one section per root question when roots have subquestions or substantial shared context, and reserve neutral numeric ranges such as `Questions 1-5` only for long unsectioned runs of many small standalone questions. Do not put multiple multi-part root questions into one numeric-range section, reorder questions, or invent difficulty labels.",
    "- Omit cover-page and administration boilerplate from the visible worksheet when it is not needed to understand or grade the questions (for example invigilator instructions, pencil rules, venue/event text, or generic timing rules). Keep scoring rules or provenance in references only when needed.",
    "- Do not put literal escaped newline text like `\\n` in visible prompts. For multiline source layouts, use real Markdown line breaks. For arranged arithmetic, grids, or layout-critical text, prefer a Markdown table or a clean crop over raw LaTeX array/tabular environments that may render as red source text.",
    "- When writing `grader/output/sheet.json` directly, remember it must be valid JSON: either avoid LaTeX backslash syntax in prompt strings or JSON-escape every backslash as `\\\\` (for example `\\\\(y = mx + c\\\\)`). Prefer plain Unicode math text when it is readable.",
    "- Place tables and figures at the nearest source-faithful level: inside the root `group.prompt` only when they appear before the first subpart or are genuinely shared by all child subparts, and inside the specific child prompt when introduced immediately before or inside one subquestion. Do not hoist child-specific figures/tables to the section start or parent group just because the renderer accepts Markdown there.",
    "- For text-selectable PDFs, run `extract_pdf_reference_text` once before bulk visual inspection and use it as a navigation/transcription aid. It is not a substitute for rendered-page checks of figures, labels, tables, or layout.",
    "- The workspace and PDF tools are available in this run. Do not say you cannot access the local PDF or ask the user to upload it again until you have tried workspace-relative paths from `grader/uploads/index.json` with `extract_text`, `extract_pdf_reference_text`, `pdf_to_images`, and `view_image` as appropriate.",
    "- Keep labels attached to their own artifacts: put `Figure N` caption text immediately adjacent to the `Figure N` crop, and put `Table N` caption text immediately adjacent to the `Table N` Markdown table. Do not place a table between a figure caption and the figure image. Do not repeat the same figure/table image in later subquestions; render it once at the first source-faithful location, then refer to it with an anchor link such as `[Figure N](#figure-n)` / `[Table N](#table-n)` or with `Figure N above` / `Table N above` when the artifact is directly above.",
    "- Before publishing, grep your transcription for named source references such as `Figure 9` or `Table 5`. Every named figure used by included questions must be present in the worksheet with a linked crop near that label, and every extracted text/numeric table must be transcribed as Markdown near that label.",
    "- When a figure or diagram matters, crop it from the source into `grader/output/assets/...` and reference the final worksheet crop as a `.jpg` workspace-relative clickable image link. `publish_sheet` normalizes linked worksheet crops to JPEG with max 512 px on either axis, so prefer `.jpg` final asset names such as `[![Figure 1](grader/output/assets/q1-figure-1.jpg)](grader/output/assets/q1-figure-1.jpg)`. Crop around the visual artifact itself; transcribe printed captions/stems such as `Figure 1 shows...` into Markdown and exclude duplicated caption/question text from the crop unless those words are part of a visual label, axis, or legend. The worksheet renders artifact labels separately, so exclude standalone `Figure N` / `Table N` captions from the crop unless removing the caption would clip required visual content. Use `trim_image` only after a complete crop; never trim away labels, axes, option text, or table cells, and leave a small clean margin so question-relevant ink does not touch the image edge. Avoid crops with large empty internal whitespace: the target visual should occupy most of the crop while still keeping a safe clean margin.",
    '- For figure isolation/refinement, use a rectangular crop workflow: do not do mask segmentation; produce one clean rectangular crop per target figure. Start from a previous bad crop if it contains the whole figure, otherwise use the full source page; inspect both the selected base image and its coordinate-grid overlay with `view_image`; choose one pixel bbox in that base image\'s coordinate space; prefer a small safe white margin over clipping; reject crops that include surrounding question text, mark text, answer lines, page borders, next-question content, or standalone Figure/Table captions already rendered by the worksheet. When documenting a crop-refinement decision, use JSON shape `{ "cropBase": "badCrop" | "fullPage", "bbox": { "left": number, "top": number, "right": number, "bottom": number }, "reasoning": "brief edge-by-edge explanation", "risks": [] }`, with origin at top-left and right/bottom exclusive. Apply the box with `crop_image` using `bboxPixels` on the selected base image.',
    "- When crop bounds are uncertain, use `propose_crop_bbox_with_fresh_agent` with the poor crop/full page/grid overlay paths so a fresh visual agent follows the rectangular JSON workflow and returns one bbox. You may also use `extract_pdf_diagrams` as a Gemini-assisted coarse bounding-box proposal or cross-check. Treat model-suggested boxes as proposals only; expand/refine them when labels, axes, legends, options, or table cells are near an edge. If one manual crop-and-view correction for the same target is still clipped/noisy/uncertain, call `propose_crop_bbox_with_fresh_agent` or `extract_pdf_diagrams` before spending more turns on hand-tuned crop boxes.",
    "- Use `pad_image` only for a crop that has already passed fresh visual validation and only needs a clean white border after `publish_sheet` reports dark edge contact. Never use `pad_image` to fix a `validate_crop_with_fresh_agent` failure, missing content, clipping, unrelated text, or a duplicated standalone caption; recrop from the high-resolution source page and validate again.",
    "- Use at most six `crop_image` attempts for any one output asset before switching strategy. After that, call `propose_crop_bbox_with_fresh_agent` or `extract_pdf_diagrams` for the source page/target label if you have not already used them. Do not publish a crop-validation report that records unresolved failures.",
    "- Final figure crops must show the complete intended visual, not a mid-diagram fragment and not a different neighbouring figure. If a crop starts inside a diagram, cuts off vertices/labels/axes/table borders/options, or includes a separate neighbouring diagram that would confuse the question, recrop from the high-resolution page image with wider bounds and recheck.",
    "- References and transcription files are audit trails, not substitutes for visible worksheet context. If a question prompt mentions a diagram, figure, graph, chart, map, network, photo, or other answer-critical visual, the visible worksheet prompt/group prompt itself must link the final crop near that text. Do not write `see source transcription/reference` instead of showing the crop.",
    "- For objective questions whose answer choices are diagrams, treat the option image as high-risk: either crop one complete options block or separate complete option crops, but every candidate label and every option diagram/shape must be fully visible. Do not publish crops where a candidate option is cut off at the crop edge or where the crop includes clipped neighbouring prompt text that should instead be transcribed.",
    "- Never validate option-diagram crops as `top/bottom portions`, `used together`, or partial fragments. A final option crop must contain whole candidate labels and whole candidate diagrams; if a row does not fit, recrop a larger complete options block or create one complete crop per candidate.",
    "- For final figure/table crops, validate the crop with a fresh visual pass before publishing: inspect representative crops at the highest available/original detail in the main agent, compare them with the question context, and call `validate_crop_with_fresh_agent` separately for each final linked figure/image crop. The fresh reviewer must use `view_image`, transcribe all visible text in the crop, and then judge whether all question-relevant visual information is included, duplicated caption/question/table text is excluded, unrelated visible text/non-target ink is absent, and no required content touches or is clipped by an edge. Missing required information or clipped option diagrams are much worse than including slight extra whitespace.",
    "- If the sheet has many final linked crops, do not run a serial main-agent `view_image` loop over every crop. The main agent may spot-check representative crops, but crop validation itself must still be one fresh-context visual-agent review per final figure/image crop so every crop is reviewed independently.",
    "- Do not publish known-failed crop validation. For uncertain crops, or after any `validate_crop_with_fresh_agent` result reports `pass/fail: fail` or a blocking issue, stop hand-guessing coordinates and use the bad-crop/full-page/grid workflow through `propose_crop_bbox_with_fresh_agent`; apply the returned rectangle with `crop_image`, then validate again with `validate_crop_with_fresh_agent`.",
    "- If an image tool reports that the pre-publish image-edit budget was exceeded, immediately stop guessing crop boxes for that output. Use existing validated crops only; do not work around the budget by linking full-page/page fallback images, publishing a crop-validation file with unresolved failures, or relabelling failed/noisy crop validation as passing.",
    "- Act on crop-review feedback before publishing: any reported clipping, missing label/axis/option/table cell, wrong association, broad full-page fallback, noisy neighbouring content, or medium/high severity issue must be recropped from the high-resolution page image and rechecked. If feedback says content is clipped, expand in the clipped direction first; do not fix it by trimming tighter. Do not treat a crop-validation report as a checkbox if it found an unresolved issue.",
    "- If you use `crop_image` or `trim_image` on a linked worksheet asset after writing `grader/output/crop-validation.md`, the validation is stale: rerun the fresh-context crop check for the final asset and rewrite `crop-validation.md` before publishing. `pad_image` is allowed only after a completed passing validation because it only adds a white border.",
    "- Record crop validation in `grader/output/crop-validation.md`: list each linked crop path, the source question/figure/table label, `fresh-context subagent checked: yes` for that final crop, the reviewer-visible text transcribed from the crop, exact `pass/fail: pass|fail`, whether all question-relevant content is visible, whether duplicated caption/question/table text was excluded unless it is part of a visual label/axis/legend, whether unrelated visible text or non-target ink is present, whether edge clipping/content touching an edge is present, and whether page borders/separator lines/answer lines/neighbouring-question fragments are present. Use the `reviewMarkdown` returned by `validate_crop_with_fresh_agent` as the basis for these records. Do not publish linked crop assets without this per-image validation record.",
    "- If a figure area contains a text/numeric table that can be transcribed, transcribe that table as Markdown in the worksheet even if you also include a crop for surrounding visual context.",
    "- Keep the user-facing run summary concise, derived from the uploaded content, and free of IDs, paths, and tool/process narration.",
    "- Keep worksheet metadata natural and non-redundant: `sheet.subject`, `sheet.level`, `sheet.title`, `sheet.subtitle`, `presentation.title`, `presentation.subtitle`, and `presentation.footer` are rendered together, so do not repeat the same paper name, level word, subject word, or phrase across them. `sheet.level` must be a compact tier/level only, not the full paper or competition title. Avoid suffixes like `question paper` in `sheet.title` when the subtitle/provenance already makes the source type clear.",
    "- `presentation.footer` must be source/provenance identity only, not a process label. Do not write `Question paper transcription`, `transcription`, `OCR`, `artifact`, or similar wording. For identified public papers, prefer concise mechanical identifiers such as exam board/qualification, tier, paper code/name, and session (for example `AQA GCSE Biology · Higher Tier Paper 1H · June 2023`).",
    '- Do not stop after extraction, page inspection, crop review, or writing draft files. The run is not complete until `publish_sheet({})` has returned `status: "published"`.',
    "- The run summary JSON must use the current publish shape: `totals.awardedMarks`, `totals.maxMarks`, `presentation.title`, `presentation.subtitle`, `presentation.summaryMarkdown`, `presentation.footer`, and `sheet.filePath`.",
    `- In ${summaryPath}, set \`sheet.filePath\` exactly to \`${sheetPath}\`; publish_sheet rejects summaries that omit this field or point at any other path.`,
    "- Do not write legacy flat summary keys such as `title`, `subtitle`, `bodySummaryMarkdown`, or `footerProvenance` at the top level of `grader/output/run-summary.json`.",
    "- Do not write `request.json`, `brief.md`, attachment metadata, planning JSON, answer lists, or process summaries into `grader/output/sheet.json` or `grader/output/run-summary.json`; those outputs must match the worksheet report and run-summary schemas only.",
    "- Do not use `generate_json` for `grader/output/sheet.json` or `grader/output/run-summary.json`; `generate_json` is for lesson-output schemas, and `request.json` / `grader/task.md` are not schemas. Write grader JSON directly and let `publish_sheet` validate it.",
    "- Final output must be one worksheet JSON artifact that the paper-sheet UI can render directly.",
    "- The worksheet artifact must contain supported sheet question types (`group`, `answer_bank`, `fill`, `cloze`, `mcq`, `lines`, `calc`, `match`, `spelling`, `flow`), the student's submitted answers, per-question awarded marks in `review.questions[questionId].score` when student answers are present, concise review notes for unresolved/incorrect work, and reference markdown for auditing. Correct/resolved questions may use an empty per-question `note` because completed feedback cards are hidden in the student UI.",
    "- Follow the real worksheet schema for structured types: `calc` requires `inputLabel` and `unit`, `lines` requires a positive `lines` count, and `mcq` in `labels_only` mode requires each option to have a source-faithful `label` such as `A`, `B`, `C`, or `D`. Use `full_options` when labels alone would not be enough.",
    '- For source MCQs whose choices are only diagram labels or positions, use `displayMode: "labels_only"` with `options[].label` and omit `options[].text` (or set it to `""`). Do not invent placeholder text such as `Option A`; the diagram/prompt already carries the meaning.',
    "- Do not duplicate MCQ choice labels in the prompt when choices are already represented by `options[]`: remove bare `(A)`, `(B)`, `(C)`, `(D)` lines from prompt text unless each line includes source option content that is not otherwise represented.",
    '- Use section-level hook/introduction blocks only with the exact shape `{ type: "hook", text: "..." }`; otherwise put shared stems, tables, and figures in `group.prompt` or a question prompt.',
    "- Do not emit empty strings for optional URL fields such as `references.paperUrl` or `references.markSchemeUrl`; omit the field unless you have a real non-empty URL.",
    "- `review.message` should be a short learning summary, not just a duplicate of the numeric score; the UI already renders `score.got / score.total` separately.",
    "- Review notes are first tutor messages, not answer-key reveals: for missing or incorrect work, give a targeted cue, contrast, or next step before exposing the final answer. Do not write notes like `The correct answer is ...` as the opening feedback unless the prompt is already fully resolved.",
    "- For unresolved feedback, do not compress the mark scheme into the hint by naming the missing final concept, mechanism, data comparison, or answer path. Prefer a question or source-navigation cue such as `Which feature in Figure 4 changes between the two samples?` over `Link the larger muscles to stronger contractions`, and `What trend do the two sources show?` over `Compare how much blue and green light chlorophyll absorbs`.",
    "- Use fresh agents selectively: keep short routine problems in the main agent. Generic `spawn_agent` is not available in grader runs for intake/file reading/transcription. Use `propose_crop_bbox_with_fresh_agent` for uncertain image-crop bbox planning, and review every final linked figure/image crop through the dedicated `validate_crop_with_fresh_agent` tool, one crop per call.",
    "- For long papers with many figures or diagram-option crops, keep the main context small: do not open every final crop at full resolution in the main agent. Send each final linked figure/image crop to `validate_crop_with_fresh_agent` with workspace paths and question context, then fix only the crops those reviewers flag.",
    "- Do not delegate long runs of short objective/MCQ items just to check answer keys; use official references when allowed, otherwise verify in the main agent.",
    "- Do not call generic `spawn_agent` in grader runs; use `validate_crop_with_fresh_agent` for crop visual validation and the main agent for workspace files.",
    "",
    "Deliverables:",
    "1) Write `grader/output/transcription.md` from a transcription-first extraction pass, then normalize student work into numbered statements/sentences (not a summary)",
    `2) Write one worksheet JSON file at ${sheetPath} that the sheet UI can render directly`,
    `3) Write ${summaryPath} with this exact top-level shape: \`{ "totals": { "awardedMarks": number, "maxMarks": number }, "presentation": { "title": string, "subtitle": string, "summaryMarkdown": string, "footer": string }, "sheet": { "title": string, "filePath": "${sheetPath}" } }\`, plus optional paper metadata only when known`,
    `4) Before the first publish attempt, verify that both ${sheetPath} and ${summaryPath} exist and reflect the final graded worksheet`,
    "5) Call publish_sheet({}) to validate and publish the worksheet artifact; if it fails, fix the files and retry until it succeeds",
    "6) Do not send a normal final response before publishing. If you catch yourself about to answer the user, instead keep using tools, write the required files, call publish_sheet, then call done.",
    "7) When official references are unavailable or disallowed, derive solutions at the student's level where reasonable and call done with that same short user-facing markdown summary after publish_sheet succeeds",
  ].join("\n");
}
