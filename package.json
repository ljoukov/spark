{
  "name": "spark-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.8",
  "workspaces": [
    "proto",
    "web",
    "packages/*",
    "eval"
  ],
  "scripts": {
    "postinstall": "bun --cwd=web run prepare || true",
    "format": "bash scripts/format-swift.sh",
    "generate": "bun --cwd=proto run generate",
    "repo:format": "bun --cwd=eval run format && bun --cwd=web run format && bun --cwd=packages/llm run format && bun --cwd=packages/schemas run format && bun --cwd=packages/proto run format && bun run format",
    "repo:lint": "bun --cwd=eval run lint && bun --cwd=web run lint && bun --cwd=packages/llm run lint && bun --cwd=packages/schemas run lint && bun --cwd=packages/proto run lint",
    "repo:typecheck": "bun --cwd=eval run typecheck && bun --cwd=web run typecheck && bun --cwd=packages/llm run typecheck && bun --cwd=packages/schemas run typecheck && bun --cwd=packages/proto run typecheck",
    "repo:check": "bun run repo:format && bun run repo:lint && bun run repo:typecheck",
    "check": "bun run repo:check"
  }
}
